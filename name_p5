#include <iostream>
#include <Eigen/Dense>

int main() {
    Eigen::Vector2d r;
    Eigen::Vector2d s;
    Eigen::Vector2d u;
    Eigen::Vector2d v;
    Eigen::Vector2d w;

    // Initialize vectors r, s, u, v, and w

    double dot_product_rs = r.dot(s);
    double dot_product_uv = u.dot(v);

    std::cout << "Dot Product of r and s: " << dot_product_rs << std::endl;
    std::cout << "Dot Product of u and v: " << dot_product_uv << std::endl;

    return 0;
}
